    //add function;
    
    function add(){

    let first = prompt("Give first number");
    let second = prompt("Give second number");
    let result = +first + +second;
    alert (`Answer: ${result}`);
}

    //subtract function;

    function subtract(){
        let first = prompt("Give first number");
        let second = prompt("Give second number");
        let result = +first - +second;
        alert (`Answer: ${result}`);
}

    //multiply function;

    function multiply(){

        
        let first = prompt("Give first number");
        let second = prompt("Give second number");
        let result = +first * +second;
        alert (`Answer: ${result}`);  
    }

    //divide function;

        function divide(){
            let first = prompt("Give first number");
            let second = prompt("Give second number");
            let result = +first / +second;
            alert (`Answer: ${result}`);   
           
        }
         